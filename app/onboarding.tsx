/**
 * Onboarding route - Entry point for the onboarding flow
 */

import React from 'react';
import { OnboardingScreen } from '@/features/auth';

export default function OnboardingRoute() {
  return <OnboardingScreen />;
}